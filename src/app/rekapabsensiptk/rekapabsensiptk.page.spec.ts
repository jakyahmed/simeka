import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RekapabsensiptkPage } from './rekapabsensiptk.page';

describe('RekapabsensiptkPage', () => {
  let component: RekapabsensiptkPage;
  let fixture: ComponentFixture<RekapabsensiptkPage>;

  beforeEach(async(() => {
    fixture = TestBed.createComponent(RekapabsensiptkPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  }));

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
